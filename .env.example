# Pegue esses valores no Supabase: Settings -> API
# São seguros para expor no navegador (por isso o prefixo NEXT_PUBLIC_) —
# quem protege os dados de verdade é o Row Level Security (RLS), não o sigilo dessas chaves.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
