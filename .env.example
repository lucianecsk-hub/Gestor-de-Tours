# Preenchido automaticamente pela Vercel quando você cria o banco (Storage -> Marketplace ->
# Neon -> Create) e conecta ele ao projeto. O nome da variável pode vir como DATABASE_URL
# ou algo como POSTGRES_URL / DATABASE_URL_UNPOOLED — use o valor principal de conexão.
DATABASE_URL=
